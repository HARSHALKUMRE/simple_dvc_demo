from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.0",
    description = "its a wine Q package",
    author = "HARSHALKUMRE",
    packages = find_packages(),
    license = "MIT"
)